<div>
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <input type="text">
    <button class="btn btn-primary">Submit</button>
  </p>
</div>
